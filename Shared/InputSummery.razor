@if (PersonalDataModel is not null)
{
	<div class="form-group   " id="@Id">
		<CustomTextArea @bind-Value="PersonalDataModel.Summary" class="textinput form-control" maxlength="@MaxLength" placeholder="Enter Your Summary (Optional)"></CustomTextArea>
		<ValidationMessage For="@(() => PersonalDataModel.Summary)" />
	</div>
}
